<%= link_to "Child Profile", child_path(@child), class: 'btn btn-info' %>

<div>
  <h1> Skills </h1>
  <% @recent_accomplishments.each do |accomplishment| %>
    <%= accomplishment.skill.title %> <br>
  <% end %>

</div>
