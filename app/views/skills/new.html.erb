<% if @accomplishment.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@accomplishment.errors.count, "error") %> prohibited this attraction from being saved:</h2>

    <ul>
    <% @accomplishment.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>


<div>
  <h1> Add New Skill </h1>
  <%= form_for @accomplishment, url: child_skills_path(@child) do |f| %>
    <%= f.collection_select :skill_id, Skill.all.order(:title), :id, :title, include_blank: 'Choose Skill ' %>
    <%= f.text_field :comment%> <br>

    <%= f.submit 'Add Skill', class: 'btn btn-primary' %>
  <% end %>
</div>
